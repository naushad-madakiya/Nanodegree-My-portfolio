<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->

    </style>

    <style name="protfolioButton" parent="android:TextAppearance.Medium">
        <item name="android:background">@drawable/my_app_button_selector</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:layout_marginTop">@dimen/myapp_button_margin</item>
        <item name="android:layout_marginBottom">@dimen/myapp_button_margin</item>
    </style>

    <style name="capstoneButton" parent="android:TextAppearance.Medium">
        <item name="android:background">@drawable/capstone_button_selector</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:layout_marginTop">@dimen/myapp_button_margin</item>
        <item name="android:layout_marginBottom">@dimen/myapp_button_margin</item>
    </style>

</resources>
